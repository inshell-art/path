name: Ops Tests (CI)

on:
  workflow_dispatch:
  pull_request:
    paths:
      - "ops/**"
      - "tests/**"
      - "schemas/**"
      - "policy/**"
      - ".github/workflows/ops_bundle.yml"
      - ".github/workflows/ops_tests.yml"

jobs:
  lock-inputs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run lock-inputs regression tests
        run: ./ops/tests/test_lock_inputs.sh

  postconditions-mode:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run postconditions mode tests
        run: ./tests/postconditions_mode.sh

  bundle-workflow-inputs:
    runs-on: ubuntu-latest
    permissions:
      contents: read
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5
      - name: Run bundle workflow locked-input tests
        run: ./tests/bundle_workflow_inputs.sh
