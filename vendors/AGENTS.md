# AGENTS (vendors/)

Overrides root guidance:
- Vendored code is read-only unless explicitly requested.
- Prefer updating via upstream/submodule and bumping the pointer rather than editing files here.
